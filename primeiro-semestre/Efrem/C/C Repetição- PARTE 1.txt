PARTE 1
1.
#include <stdio.h>

int main() {
    int i;
    
    for (i=1; i>10; i++){
        printf("%d", i);
    }
    
    return 0;
}


2.
#include <stdio.h>

int main() {
    int i;
    for (i=0; i<=20; i+=2){
        printf("%d ", i);
    }
    return 0;
}


3.
#include <stdio.h>

int main() {
    int i, n;
    
    printf("Digite um numero: ");
    scanf("%d", &n);
    
    for (i=1; i<=10; i++){
        printf("%d x %d = %d\n", i, n, i*n);
    }
    return 0;
}


4.
#include <stdio.h>

int main() {
    int i, n, soma;
    
    soma = 0;
    for (i=1; i<=5; i++){
        printf("Digite um numero: ");
        scanf("%d", &n);
        soma = soma + n;
    }
    
    printf("Soma total: %d", soma);
    return 0;
}


5.
#include <stdio.h>

int main() {
    int i, n, maior, primeiro;
    
    primeiro = 1;
    for (i=1; i<=5; i++){
        printf("Digite um numero: ");
        scanf("%d", &n);
        if (primeiro){
            maior = n;
            primeiro--;
        }
        if (n > maior){
            maior = n;
        }
    }
    
    printf("Maior: %d", maior);
    return 0;
}


6.
#include <stdio.h>

int main() {
    int i, n, media;
    
    media = 0;
    for (i=1; i<=10; i++){
        printf("Digite um numero: ");
        scanf("%d", &n);
        media = media + n;
    }
    
    printf("Media: %d", media/10);
    return 0;
}


7.
#include <stdio.h>

int main() {
    int i, n, cont;
   
   cont = 0;
    printf("Digite um numero: ");
    scanf("%d", &n);
    
    for (i=1; i<=n; i++){
       if (n % i == 0){
       cont++;
       }
    }
    
    printf("Quantidade de divisores de %d: %d", n, cont);
    return 0;
}


8.
#include <stdio.h>

int main() {
    int i, n, fat;
   
   fat = 1;
    printf("Digite um numero: ");
    scanf("%d", &n);
    
    for (i=n; i>0; i--){
       fat = fat * i;
    }
    
    printf("Fatorial de %d: %d", n, fat);
    return 0;
}


9.
#include <stdio.h>

int main() {
    int i, n, soma;
   
   soma = 0;

    do {
        printf("Digite um numero: ");
        scanf("%d", &n);
        soma = soma + n;
    } while (n != 0);
    
    printf("Soma total: %d", soma);
    return 0;
}


10.
#include <stdio.h>

int main() {
    int i;
    
    for (i = 0;i <=100;i+=4){
        printf("%d ", i);
    }
   return 0;
}


11.
#include <stdio.h>

int main() {
    int i, n, soma;
   
   soma = 0;

    do {
        printf("Digite um numero: ");
        scanf("%d", &n);
        soma = soma + n;
    } while (soma <= 100);
    
    printf("Soma ultrapassou 100");
    return 0;
}


12.
#include <stdio.h>

int main() {
    int i, n, soma;
   
    soma = 0;
    printf("Digite um numero: ");
        scanf("%d", &n);
    
    for (i=1;i<=n;i++) {
        printf("%d ", i);
    }
    
    return 0;
}


13.
#include <stdio.h>

int main() {
    int i, n, maior;
   
    maior = 0;

    for (i=1;i<=10;i++) {
        printf("Digite um numero: ");
        scanf("%d", &n);
        if (n >= 18){
            maior++;
        }
    }
    printf("Maiores de idade: %d", maior);
    
    return 0;
}


14.
#include <stdio.h>

int main() {
    int i, n;
   
    printf("Digite um numero: ");
    scanf("%d", &n);
    
    printf("Divisores de %d:\n", n);
    for (i=1; i<=n; i++){
       if (n % i == 0){
       printf("%d ", i);
       }
    }
    return 0;
}


15.
#include <stdio.h>

int main() {
    int i, n1, n2;
   
    printf("Digite um numero: ");
    scanf("%d", &n1);
    printf("Digite outro numero: ");
    scanf("%d", &n2);
    
    if (n1 > n2){
        for (i=n2; i<=n1; i++){
        printf("%d ", i);
        }
    } else if (n2 > n1){
        for (i=n1; i<=n2; i++){
        printf("%d ", i);
        }
    } else {
        printf("Os numeros são iguais");
    }
    return 0;
}


16.
#include <stdio.h>

int main() {
    int i, nota, acima;
   
    acima = 0;
    
    for (i=1; i<=5; i++){
        printf("Digite a nota: ");
        scanf("%d", &nota);
        
        if (nota >= 7){
            acima++;
        }
    }
       
    printf("No total, %d aluno(s) acima da media", acima);
       
    return 0;
}


17.
#include<stdio.h>

int main()
{
    int i;
    
    for (i=100;i>=0;i-=2){
    printf("%d ", i);
    }
    return 0;
}


18.
#include <stdio.h>

int main() {
    int i, n, pos, neg, zero;
   
   pos = 0;
   neg = 0;
   zero = 0;
   
    for (i=1; i<=10; i++){
        printf("Digite um numero: ");
        scanf("%d", &n);
        
        if (n > 0){
            pos++;
        } else if (n < 0) {
            neg++;
        } else {
            zero++;
        }
    }
       
    printf("Positivos: %d\nNegativos: %d\nZeros: %d", pos, neg, zero);
       
    return 0;
}


19.
#include <stdio.h>

int main() {
    int i, anterior, proximo, soma;
   
    anterior = 0;
    proximo = 1;   
    
    printf("%d %d ", anterior, proximo);
    for (i=1; i<=10; i++){
        soma = anterior + proximo;
        printf("%d ", soma);
        anterior = proximo;
        proximo = soma;
        
    }
    return 0;
}


20.
#include <stdio.h>

int main() {
    int i, n, menor, primeiro;
  
    primeiro = 1;
    do {
        printf("Digite um numero: ");
        scanf("%d", &n);
        if (n > 0){
            if (primeiro){
            menor = n;
            primeiro--;
            }
            if (n < menor){
            menor = n;
            }
        }
    } while (n > 0);
    
    printf("Menor numero digitador: %d", menor);
    return 0;
}